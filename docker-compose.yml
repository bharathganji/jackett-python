version: '3.8'

services:
  jackett-python:
    build: .
    ports:
      - "9889:9889"
    environment:
      - JACKETT_API_URL=https://your-jackett-instance.com
      - API_KEY=your_api_key