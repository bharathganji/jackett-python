version: '3.8'

services:
  jackett-python:
    build: .
    ports:
      - "9889:9889"
    environment:
      - JACKETT_API_URL=${JACKETT_API_URL}
      - API_KEY=${API_KEY}
      - PORT=${PORT}
version: '3.8'

services:
  jackett-python:
    build: .
    ports:
      - "9889:9889"
    environment:
      - JACKETT_API_URL=${JACKETT_API_URL}
      - API_KEY=${API_KEY}
      - PORT=${PORT}